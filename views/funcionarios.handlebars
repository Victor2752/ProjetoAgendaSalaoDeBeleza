<div class="container">
    <div class="containerTituloGrande">
        <h1 class="tituloGrande">Funcionários</h1>
    </div>

    <div class="containerAdicionarOuRemover">

        <form action="/funcionarios/adicionar" method="POST" class="containerAdicionar">
            <div class="containerTituloPequeno">
                <h2 class="tituloPequeno">Adicionar Funcionário</h2>
            </div>
            <div class="containerAcao">
                <div class="containerInput">
                    <label for="inputFuncionario">Nome do funcionário:</label>
                    <input type="text" class="input" name="inputFuncionario" required>
                </div>
                <div class="containerInput">
                    <label for="selectFuncao">Nome da função:</label>
                    <select class="select" name="selectFuncao" required>
                        <option value="0">Selecione a função</option>
                        <!--comando do handlebars: para cada funcionário dentro do banco, crie um option
                        com o value sendo o id e com o conteúdo sendo o nome do funcionário-->
                        {{#each funcoes}}
                        <option value="{{dataValues.nome}}">{{{dataValues.nome}}}</option>
                        {{/each}}
                    </select>
                </div>
                <button type="submit" class="btn">Adicionar</a>
            </div>
        </form>

        <form action="/funcionarios/remover" method="POST" class="containerRemover">
            <div class="containerTituloPequeno">
                <h2 class="tituloPequeno">Remover Funcionário</h2>
            </div>
            <div class="containerAcao">
                <div class="containerInput">
                    <label for="selectFuncionario">Nome do funcionário:</label>
                    <select class="select" name="selectFuncionario" required>
                        <option value="0">Selecione o funcionário</option>
                        <!--comando do handlebars: para cada funcionário dentro do banco, crie um option
                        com o value sendo o id e com o conteúdo sendo o nome do funcionário-->
                        {{#each funcionarios}}
                        <option value="{{dataValues.id}}">{{{dataValues.nome}}}</option>
                        {{/each}}
                    </select>
                </div>
                <button type="submit" class="btn">Remover</button>
            </div>
        </form>

    </div>

</div>